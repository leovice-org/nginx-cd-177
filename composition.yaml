apiVersion: composition.krateo.io/v0-1-0
kind: NginxServer
metadata:
  name: nginx-server-composition-1
  namespace: krateo-system
spec:
  image:
    repository: nginx
    tag: "1.25.3"
  service:
    type: ClusterIP
